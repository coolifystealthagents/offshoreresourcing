import { Header, Footer, CTA } from '../../components';
import { services, site } from '../../data';
export function generateStaticParams(){return services.map(s=>({slug:s.slug}))}
export async function generateMetadata({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const s=services.find(x=>x.slug===slug);return {title:s?.title||'Service',description:s?.desc}}
export default async function Service({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const s=services.find(x=>x.slug===slug)||services[0];return <><Header/><main><section className='service-hero'><div className='container two'><div><p className='eyebrow'>{site.brand} service</p><h1>{s.title}</h1><p className='lead'>{s.desc}</p><a className='btn' href='/contact'>Plan this role</a></div><div className='hero-card'><img src={site.serviceImage} alt={`${s.title} offshore service team`}/></div></div></section><section className='section'><div className='container cards'><div className='card'><h3>Good first tasks</h3><ul>{s.bestTasks.map((item)=><li key={item}>{item}</li>)}</ul></div><div className='card'><h3>Checks to keep</h3><ul>{s.controls.map((item)=><li key={item}>{item}</li>)}</ul></div><div className='card'><h3>First week</h3><ul>{s.firstWeek.map((item)=><li key={item}>{item}</li>)}</ul></div></div></section><CTA/></main><Footer/></>}
