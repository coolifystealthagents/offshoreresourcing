import {site,services,blogPosts} from '../data';import {getDocuments} from '../../lib/content';export function GET(){const base=`https://${String(site.domain).toLowerCase()}`;const pages=Math.max(1,Math.ceil(blogPosts.length/20));const research=getDocuments('research');const alternatives=getDocuments('alternatives');const paths=['','/services','/blog','/research','/alternatives','/contact','/privacy','/terms','/cancellation-policy',...services.map(s=>`/services/${s.slug}`),...blogPosts.map(p=>`/blog/${p.slug}`),...research.map(p=>`/research/${p.slug}`),...alternatives.map(p=>`/alternatives/${p.slug}`),...Array.from({length:Math.max(0,pages-1)},(_,i)=>`/blog/page/${i+2}`)];const body=paths.map(x=>`<url><loc>${base}${x}</loc></url>`).join('');return new Response(`<?xml version="1.0" encoding="UTF-8"?><urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">${body}</urlset>`,{headers:{'content-type':'application/xml'}})}
